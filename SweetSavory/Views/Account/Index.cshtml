@{
  Layout = "_Layout";
}

@using System.Security.Claims

<div class="container my-4">
    <h2 class="text-center">Authentication with Identity</h2>
    <hr />
    <div class="d-flex flex-column align-items-center">
        <div class="d-flex flex-row justify-content-center">
            @if (User.Identity.IsAuthenticated)
            {
                <p>Hello @User.Identity.Name!</p>
                @using (Html.BeginForm("LogOff", "Account"))
                {
                    <input type="submit" class="fancyBtn mx-3" value="Log out" />
                }
            }
            else
            {
                <p class="fancyBtn mx-3">@Html.ActionLink("Register", "Register")</p>
                <p class="fancyBtn mx-3">@Html.ActionLink("Log in", "Login")</p>
            }
        </div>
    </div>
</div>